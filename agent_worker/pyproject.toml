[project]
name = "my-voice-agent-hermes-worker"
version = "0.1.0"
description = "LiveKit voice worker that routes final user turns to Hermes Director"
requires-python = ">=3.11"
dependencies = [
  "livekit-agents>=1.3.0",
  "livekit-plugins-silero>=1.3.0",
]

[tool.uv]
package = false
